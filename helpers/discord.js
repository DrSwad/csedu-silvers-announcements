export default async function announceInDiscord(message) {
  try {
    // code for sending `message` to some chat
    // chat ID (or whatever is required to send messages via discord API) can be hardcoded for now
  } catch(err) { console.error(err) }
}